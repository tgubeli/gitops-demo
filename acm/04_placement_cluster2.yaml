apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  labels:
    app: httpd
  name: apache-cluster2
  namespace: simple-app
spec:
  clusterSelector:
    matchLabels:
      clusterid: cluster2
